name: server
description: A server for the study app
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.3
  shelf_cors_headers: ^0.1.5
  sqlite3: ^2.3.0
  path: ^1.8.3

dev_dependencies:
  http: ^1.1.0
  lints: ^2.0.0
  test: ^1.21.0
